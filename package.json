{
  "name": "paperclip-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run deploy:prod",
    "deploy:prod": "astro build && npx wrangler pages deploy dist --project-name paperclip-website --branch master --commit-dirty=true",
    "deploy:canary": "astro build && npx wrangler pages deploy dist --project-name paperclip-website --branch canary --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1"
  }
}
